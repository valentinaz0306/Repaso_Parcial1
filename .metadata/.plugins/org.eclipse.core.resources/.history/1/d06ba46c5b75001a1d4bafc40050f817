package controller;

import java.util.Comparator;

public class CompareRaza implements Comparator<Perro> {
   
	@Override
    public int compare(Perro perro1, Perro perro2) {
    	return perro1.getRaza().compareTo(perro2.getRaza());
    }
}